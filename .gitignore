# 构建期产物：上游源码由 scripts/fetch-upstream.sh 拉取，不进版本库
upstream-src/
upstream-src.commit

# 运行期数据（绝不该提交：账号凭证就在这里）
data/
upstream/
*.db
*.db-wal
*.db-shm
.session_secret
upstream.log

# 本地离线镜像包
*.tar
*.tar.gz

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# 编辑器
.vscode/
.idea/
.DS_Store
